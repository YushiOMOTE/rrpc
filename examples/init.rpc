use sample;

/// comment
struct name {
    /// comment
    x: sample::Coordinate {{0, 0}},
    /// comment
    y: vector<u32> {value},
}

/// comment
enum Mode(u32) {
    /// comment
    Flat{0},
    /// comment
    Multi,
}

/// comment
interface name(pattern) {
    /// comment
    fn create(x: string, y: u32) -> (string, bool);
}
